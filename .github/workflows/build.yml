name: Build Qt Application

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-linux:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: "6.6.3"
          host: "linux"
          target: "desktop"
          arch: "gcc_64"

      - name: Install CMake
        run: sudo apt-get install -y cmake libfuse2

      - name: Build
        run: |
          mkdir build
          cd build
          cmake -DCMAKE_BUILD_TYPE=Release ..
          cmake --build .

      - name: Install linuxdeployqt
        run: |
          wget https://github.com/probonopd/linuxdeployqt/releases/download/continuous/linuxdeployqt-continuous-x86_64.AppImage
          chmod +x linuxdeployqt-continuous-x86_64.AppImage

      - name: Package application
        run: |
          cd build/bin/Release
          ../../../linuxdeployqt-continuous-x86_64.AppImage StructuredLogViewer -appimage

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: StructuredLogViewer-linux
          path: build/bin/Release/*.AppImage

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: "6.6.3"
          host: "windows"
          target: "desktop"
          arch: "win64_msvc2019_64"

      - name: Install CMake
        run: choco install cmake --installargs 'ADD_CMAKE_TO_PATH=System' -y

      - name: Build
        run: |
          mkdir build
          cd build
          cmake ..
          cmake --build . --config Release

      - name: Package application
        run: |
          cd build/bin/Release
          windeployqt StructuredLogViewer.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: StructuredLogViewer-windows
          path: build/bin/Release/*
