# Test-level overrides.
#
# Magic numbers are common and acceptable in test code (expected values,
# test data, etc.). `cert-err58-cpp` warns about static initialization
# exceptions, acceptable in test utilities. Public member variables are
# the norm in Catch2 fixtures / sink structs — `struct { ... }` with
# exposed fields is idiomatic here. `cppcoreguidelines-owning-memory`
# fires on the `FILE *fp = std::fopen(...)` / manual `fclose` dance that
# the latency benchmark deliberately uses for direct-syscall control.
# `bugprone-unchecked-optional-access` only models a tiny set of guard
# forms (`if`/`DCHECK`/`ASSERT_TRUE`); the Catch2 / QtTest idioms used
# throughout the suite (`REQUIRE(opt.has_value()); CHECK(*opt == ...)`,
# `QVERIFY(opt.has_value()); QCOMPARE(opt.value(), ...)`) trip false
# positives on every `*opt` / `opt.value()` access, so the check is
# uniformly noise here.
# `clang-analyzer-optin.core.EnumCastOutOfRange` fires inside Catch2's
# own `ResultDisposition::Flags` bitwise-OR path (e.g. `CHECK_FALSE`
# expands to `ContinueOnFailure | FalseTest` -> `6`, which isn't one
# of the four named enumerators). The header-filter excludes
# `_deps/catch2-src`, but the path-sensitive analyzer attributes the
# warning to the test TU that triggered the trace, so the only way to
# silence the Catch2 macro noise is to disable the check on tests.
# `clang-analyzer-optin.cplusplus.VirtualCall` fires inside asio's
# `resolver_thread_pool::~resolver_thread_pool`, which calls its own
# virtual `shutdown()` because asio owns the shutdown-ordering
# invariant. It's asio's design, not ours; the test-only network
# clients construct a `resolver` to look up the peer once at startup,
# reproducing the trace. Header-filter excludes `_deps/asio-src`, but
# `llvm::Regex` (used by `-header-filter`) doesn't support the
# negative-lookahead form our regex uses, so the header-filter is a
# no-op for these files -- the check is disabled here instead.

InheritParentConfig: true

Checks: >
  -cppcoreguidelines-avoid-magic-numbers,
  -readability-magic-numbers,
  -cert-err58-cpp,
  -misc-non-private-member-variables-in-classes,
  -cppcoreguidelines-non-private-member-variables-in-classes,
  -cppcoreguidelines-owning-memory,
  -bugprone-unchecked-optional-access,
  -clang-analyzer-optin.core.EnumCastOutOfRange,
  -clang-analyzer-optin.cplusplus.VirtualCall
